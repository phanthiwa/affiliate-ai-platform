/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'shop.tiktok.com', 'shopee.co.th'],
  },
}

module.exports = nextConfig
